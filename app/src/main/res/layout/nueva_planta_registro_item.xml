<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:layout_marginBottom="8dp">

    <Spinner
        android:id="@+id/plantsAvailable"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="2"
        android:background="@drawable/background_cantidad"
        android:layout_marginTop="10dp"/>

    <EditText
        android:fontFamily="@font/my_custom_font"
        android:id="@+id/et_plant_quantity"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Cantidad"
        android:inputType="number"
        android:layout_weight="1"
        android:layout_marginStart="8dp"
        android:layout_marginTop="10dp"
        android:background="@drawable/background_cantidad"/>
    <ImageView
        android:id="@+id/eliminate_registered"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_gravity="center"
        android:layout_margin="5dp"
        android:layout_marginTop="14dp"
        android:src="@drawable/ic_eliminar2"
        android:contentDescription="Eliminar registro" />
</LinearLayout>
