<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".FragmentPlantMonitoring">

    <!-- Título de la Pantalla -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Seguimiento de la Planta"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#225E1D"
        android:fontFamily="@font/coustard"
        android:layout_gravity="top|center_horizontal"
        android:layout_marginTop="16dp" />

    <!-- RecyclerView para el historial -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerHistorial"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="80dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:scrollbars="vertical" />

    <!-- Botón para agregar una nueva entrada de seguimiento -->
    <Button
        android:id="@+id/btnAgregarSeguimiento"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal|bottom"
        android:layout_marginBottom="32dp"
        android:text="Agregar Progreso"
        android:backgroundTint="#4CAF50"
        android:textColor="#FFFFFF"
        android:padding="12dp"
        android:textSize="16sp"
        android:fontFamily="@font/coustard"
        android:elevation="4dp" />

</FrameLayout>